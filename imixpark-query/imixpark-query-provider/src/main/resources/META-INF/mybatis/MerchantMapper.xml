<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.ebatech.imixpark.query.dao.MerchantMybatisDao">

	<select id="getMerchant" parameterType="long" resultType="Merchant">
		select id, merchant_name, merchant_code, account_number, merchant_address, merchant_phone
		from ma_merchant
		where id=#{id}
	</select>

	<select id="search" parameterType="map" resultType="Merchant">
		select id, merchant_name, merchant_code, account_number, merchant_address, merchant_phone
		from ma_merchant
	</select>

</mapper> 
