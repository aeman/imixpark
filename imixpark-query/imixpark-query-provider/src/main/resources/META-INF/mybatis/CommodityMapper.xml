<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.ebatech.imixpark.query.dao.CommodityMybatisDao">

	<select id="getCommodity" parameterType="long" resultType="Commodity">
		select id, commodity_name, commodity_code, commodity_attr, commodity_price, item_no
		from ma_commodity
		where id=#{id}
	</select>

	<select id="search" parameterType="map" resultType="Commodity">
		select id, commodity_name, commodity_code, commodity_attr, commodity_price, item_no
		from ma_commodity
	</select>

</mapper> 
